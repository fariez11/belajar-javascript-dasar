// function declaration -------------------------------------------------------
    // function identifier (ParameterList opt) { FunctionBody}         // Rumus
    
    // contoh
        function tampilPesan(nama){
            alert('halo' + nama);
        }

// function expression --------------------------------------------------------
    // function identifier opt (ParameterList opt) { FunctionBOdy}     // Rumus

    // contoh 
        var tampilPesan = function (nama){
            alert('halo' + nama);
        }